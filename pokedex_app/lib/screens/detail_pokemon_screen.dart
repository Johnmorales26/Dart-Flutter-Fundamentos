import 'package:flutter/material.dart';

class DetailPokemonScreen extends StatelessWidget {
  const DetailPokemonScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const Text('PokedexScreen');
  }

}